{
  "name": "shadcn-ui-svelte",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/docs",
    "build": "npm run build --workspace=packages/components",
    "check": "npm run check --workspace=packages/components",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "prettier-plugin-svelte": "^3.4.0"
  }
}
