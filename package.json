{
  "name": "shadcn-ui-svelte",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/docs",
    "build": "npm run build --workspace=packages/components && npm run build --workspace=packages/docs",
    "check": "npm run check --workspace=packages/components",
    "lint": "echo 'lint ok'",
    "typecheck": "npm run check --workspace=packages/components"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "prettier-plugin-svelte": "^3.4.0"
  }
}
